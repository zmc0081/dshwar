# CLAUDE.md 与 ARCHITECTURE.md 的补丁清单

> **为什么是补丁而不是整份文件**:`SESSION_TASKS.md` 你上传了当前版本,我可以直接改;
> `CLAUDE.md` 和 `ARCHITECTURE.md` 我手上只有 V0.1.0 之前的初版,V0.1.0–V0.4.0 期间
> 必然已经改过。用旧版覆盖会抹掉四个版本的沉淀,所以这里只给**要插入的内容和插入位置**。
>
> 交给 Claude Code 执行即可,或人工粘贴。

---

## 一、CLAUDE.md

### 补丁 1 · 顶部版本号

```diff
- > 当前版本(正在开发): **V0.4.0**
+ > 当前版本(正在开发): **V0.4.1**
```

### 补丁 2 · 第二节「硬规则」末尾追加第 9 条

在现有第 8 条之后插入:

```markdown
9. **开源分发的构建产物不得包含任何闭源组件**——`billing-hosted` 等闭源部分必须是独立构建产物。这既是 open-core 的边界,也是 SignPath Foundation 免费签名的资格条件(不得含维护者或关联组织发布的专有代码)
```

### 补丁 3 · PR 自查清单追加一行

在 `grep -rn "process\.env" packages/` 那行之后插入:

```bash
node scripts/check-oss-purity.mjs   # 开源构建产物不含闭源组件（硬规则 9）        → 0
```

### 补丁 4 · 第七节「安全与隔离」追加两条

```markdown
- **同一用户的不同工作区之间也是隔离的**,但隔离级别与租户间相同——逻辑隔离仅适用于互相信任的场景
- **不做运行时审批弹窗**。上游 SDK 协议的 server→client 请求是死能力;审批走**策略预授权 + 事后审计**,拒绝进 `@dshwar/audit`
```

### 补丁 5 · 第八节「开源与商业边界」追加一条

```markdown
- **客户端分发**:开源版由项目签名(Windows 走 SignPath Foundation 免费通道,macOS 走 Apple Developer $99/年);**客户白牌版本由客户自己签名**——界面挂客户品牌,签名主体就该是客户。DSHWAR 只提供打包与签名的 CI 模板,不代签
```

---

## 二、ARCHITECTURE.md

### 补丁 6 · 三平面图加「端」这一层

把现有三平面图替换为:

```
┌──────────────────────────────────────────────────────────────┐
│  端  Clients                                    V0.7.0 起      │
│  Web 工作台 · Tauri 桌面壳（胖客户端）· 移动端 SDK             │
│  一份 React 代码，三个宿主；只认 OpenAPI，baseURL 可切         │
├──────────────────────────────────────────────────────────────┤
│  控制平面  Control Plane                      独立仓库/独立部署  │
│  租户 · 成员与角色 · 订阅 · 支付 · 配额策略 · 审计 · 用量看板     │
├──────────────────────────────────────────────────────────────┤
│  API 平面  Gateway                              ★ 项目护城河    │
│  运行时 API  /v1/sessions · SSE · SDK                          │
│  管理 API    /v1/admin/*  ← CMS / 开源后台 / 低代码平台直连     │
│  供给 API    /scim/v2/*   ← Keycloak / Okta / Entra 零代码同步  │
├──────────────────────────────────────────────────────────────┤
│  运行时平面  Runtime                          cordis 插件集     │
│  principal · credentials · fs · storage · session · 计量治理    │
│                              ┌─────────────────────────────┐  │
│                              │ Supervisor 进程编排 / 隔离级别 │  │
│                              └─────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│  DeepSeek Harness（上游，npm 依赖，版本锁死）                    │
└──────────────────────────────────────────────────────────────┘
```

### 补丁 7 · 新增一节「双运行模式」

放在「1.3 服务对象」之后:

```markdown
### 1.4 双运行模式

一份代码,靠 `profiles/` 切换,**不分叉出两套实现**:

| 维度 | 开源自建模式                      | 托管商业模式                |
| ---- | --------------------------------- | --------------------------- |
| 身份 | `auth-static` / `auth-jwt` 或匿名 | `auth-oidc` 接托管服务      |
| 凭据 | **用户自己的 API key,本地直连**   | 网关下发短时效 scoped token |
| 计量 | 本地记账,不上报                   | 上报托管服务,参与计费       |
| 品牌 | 中性(DSHWAR)                      | 运行期主题,配置由服务端下发 |
| 模型 | 云端 API 或**本地 Ollama**        | 走 `model-router` 网关路由  |

这解释了为什么 `credentials-multiuser` 必须支持本地直连模式——开源用户不该被强制走网关。
```

### 补丁 8 · 隔离模型章节(§2.4)追加

```markdown
**工作区维度**:自 V0.4.1 起路径模型为 `{root}/{tenantId}/{userId}/{workspaceId}`。
同一用户的不同工作区之间也做路径钉死,但**隔离级别与租户间相同**——逻辑隔离仅适用于
互相信任的场景,跨信任边界仍须进程或容器隔离。

**审批**:上游 SDK 协议的 server→client 请求是死能力,交互式审批弹窗今天做不到。
DSHWAR 采用**策略预授权 + 事后审计**:工作区设置页配置允许的工具 / 路径 / 网络 / shell,
运行时按策略放行或拒绝,拒绝进审计流。交互式审批标注为探索项,**依赖上游补齐该能力**。
```

### 补丁 9 · 路线图与风险登记

- 路线图表替换为 `SESSION_TASKS.md` 第一部分的新版本路线表(含 V0.4.1 / V0.5.5 / V0.6.5 / V0.8.0 与两条并行轨)
- 风险登记新增三条:

| #   | 风险                                                      | 影响         | 应对                                          |
| --- | --------------------------------------------------------- | ------------ | --------------------------------------------- |
| 9   | 胖客户端打包链路(Node 22 + 三个原生模块 + macOS 逐个签名) | 交付延期     | V0.7.0 内单独排两周,不混功能 Session          |
| 10  | SignPath Foundation 资格(不得含专有组件)                  | 无法免费签名 | 硬规则 9 + `check-oss-purity` CI 检查         |
| 11  | `/v1` 契约因 `workspaceId` 而变更                         | 破坏下游     | V0.4.1 Session 1 先出决策文档并显式声明兼容性 |

---

## 三、执行指令(粘给 Claude Code)

```
读取 CLAUDE.md 与 ARCHITECTURE.md，按 PATCH_NOTES.md 中的补丁 1-9 逐条应用。

要求：
- 只做插入与替换，不删除现有内容（V0.1.0-V0.4.0 期间的沉淀必须保留）
- 每条补丁应用后确认锚点唯一，若锚点已变化，报告实际内容再决定，不要猜
- 补丁 3 引用的 scripts/check-oss-purity.mjs 尚未实现，本次只加检查行，
  脚本实现放在 V0.4.1 Session 2
- 完成后报告：每条补丁的应用结果，以及任何锚点不匹配的情况
```
