# DSHWAR — 技术架构与实施路线

> 为商业 AI 应用提供产品基座。在 DeepSeek Harness 之上补齐 ToB 产品运营能力。
>
> MIT 协议 · 开发者预览 · v0.1

---

## 0. 一句话边界

> **上游做能力,DSHWAR 做归属、隔离、配额、计费、审计。**

这一句是所有架构决策的裁决标准。任何一个新包立项前先回答:它属于"能力"还是"治理"?属于能力,先确认上游三个月内不会做;属于治理,放心做——单用户场景不需要治理,上游永远不会碰。

### 与上游的关系原则

| 原则         | 说明                                                              |
| ------------ | ----------------------------------------------------------------- |
| **不 fork**  | 从 npm 消费 `@deepseek-ai/dsh-*`,像任何第三方插件作者一样         |
| **不 patch** | 需要改上游才能实现的功能,提 issue,不建 patch 目录                 |
| **单点适配** | 只有 `adapters/dsh-<version>/` 允许 import 上游内部实现           |
| **契约自有** | 对外 API 由 DSHWAR 定义与版本化,不透传上游的 pre-release 不确定性 |

> 身份互操作的完整设计见 [`IDENTITY-INTEROP.md`](IDENTITY-INTEROP.md)。

---

## 1. 产品架构

### 1.1 三平面

```
┌──────────────────────────────────────────────────────────────┐
│  控制平面  Control Plane                      独立仓库/独立部署  │
│  租户 · 成员与角色 · 订阅 · 支付 · 配额策略 · 审计 · 用量看板     │
│  与 dsh 零耦合，唯一接触点是「消费了多少 token」这一个数字        │
├──────────────────────────────────────────────────────────────┤
│  API 平面  Gateway                              ★ 项目护城河    │
│  运行时 API  /v1/sessions · SSE · SDK（TS/Python/Kotlin/Swift） │
│  管理 API    /v1/admin/*  ← CMS / 开源后台 / 低代码平台直连      │
│  供给 API    /scim/v2/*   ← Keycloak / Okta / Entra 零代码同步   │
├──────────────────────────────────────────────────────────────┤
│  运行时平面  Runtime                          cordis 插件集     │
│  principal · credentials · fs · storage · session · 记忆治理    │
│  模型路由 · 计量埋点          ┌─────────────────────────────┐  │
│                              │ Supervisor 进程编排 / 隔离级别 │  │
│                              └─────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│  DeepSeek Harness（上游，npm 依赖，版本锁死）                    │
└──────────────────────────────────────────────────────────────┘
```

**为什么控制平面必须独立部署**:计费和租户管理是标准 SaaS 控制面。做成 cordis 插件意味着 dsh 不运行就不能开账单、不能独立扩容、发票系统绑死在 0.1 rc 依赖上。它有自己的数据库、自己的发版节奏、自己的 SLA。

**为什么 API 平面是护城河**:运行时插件可替换,控制面是标准 SaaS,只有那份稳定契约是客户接进来之后换不掉的。移动端、Web 端、第三方集成全部依赖它。而上游两条通道都不能直接用——SDK 协议是 stdio JSON-RPC(移动端连不上);内置 webserver 没有 TLS 与认证,仅有一道面向浏览器的 Origin 栅栏,而**不带 `Origin` 头的请求直接放行**——那是 CSRF 防护,不是认证,凡能连到端口者即拥有完全访问权。实测见 `docs/FEASIBILITY-REPORT.md` §4.4。

### 1.2 能力地图

三类分区决定每项能力"做哪一层"。

#### 安全区 — 上游结构性永不涉足

| 能力     | 做什么                                       |
| -------- | -------------------------------------------- |
| 用户系统 | principal 传播、认证适配、per-principal 凭据 |
| 租户隔离 | 工作区路径钉死、存储键前缀、会话归属         |
| 计量计费 | token 归属、成本核算、订阅、支付             |
| 运营后台 | 成员管理、用量看板、审批流、审计查询         |
| 移动端   | 上游完全空白                                 |

#### 协作区 — 上游做能力,我们做治理

| 能力   | 上游已有                                                  | DSHWAR 只做                                 |
| ------ | --------------------------------------------------------- | ------------------------------------------- |
| 记忆   | `context/`、`compaction/`、`spill/`、`session-projection` | 归属、跨租户泄漏防护、保留与删除策略、配额  |
| 多模型 | `llm/`、`subagent/`、`workflow/`、`plan/`                 | 模型路由、预算降级、按 principal 限流与计费 |
| 存储   | `storage` + `-json` / `-sqlite` / `-domain`               | 租户维度作用域                              |
| 沙箱   | `sandbox`、`sandbox-policy`、`sandbox-windows-acl`、`e2b` | 按 principal 计算策略喂给它,不另起炉灶      |

> **记忆的纪律**:定义 `@dshwar/memory` 作为**自有契约**,先用薄实现顶上;上游引擎出来后把实现换成对它的包装,对外 API 不变。绝不自建记忆引擎。

#### 重叠区 — 复用而非竞争

| 能力     | 上游已有                                                   | 分工                  |
| -------- | ---------------------------------------------------------- | --------------------- |
| 对话界面 | `apps/web`、`client/` 三十多个 `ui-*`、`ui-slots` 槽位系统 | 复用槽位约定与组件    |
| 运营后台 | 无                                                         | 完全自研,这是产品本体 |

上游的 Web 是**单用户开发者控制台**,租户后台、用量看板、订阅页面它一个都没有。分工是清楚的。

### 1.3 服务对象

| 角色         | 用什么                           | 关心什么              |
| ------------ | -------------------------------- | --------------------- |
| 自建开发者   | 克隆开源仓库,`profiles/team.yml` | 零成本跑起来,不被绑架 |
| ToB 产品团队 | Gateway + SDK,自建控制面         | API 稳定性、隔离强度  |
| 企业采购方   | 完整基座 + 托管                  | 合规、审计、SSO、SLA  |

第一类的体验决定项目能不能活,后两类决定能不能赚钱。**任何设计不得为了后两类牺牲第一类的开箱即用。**

---

## 2. 技术架构

### 2.1 技术栈

| 平面       | 组件     | 选型                              | 理由                                                                                             |
| ---------- | -------- | --------------------------------- | ------------------------------------------------------------------------------------------------ |
| 运行时     | 语言     | TypeScript strict                 | 必须与上游同栈,贡献者门槛最低                                                                    |
|            | Node     | `^22.19.0 \|\| >=24.0.0`          | 抄上游 `engines`,不宽不窄                                                                        |
|            | 插件框架 | `@deepseek-ai/cordis` 4.0.1       | `Service` 基类与 `ctx.isolate()` 的来源                                                          |
|            | 上游     | `@deepseek-ai/dsh-*` `0.1.0-rc.6` | 精确锁版,registry 版本号与仓库根不同;子包 `latest` 标签已坏,见 `docs/FEASIBILITY-REPORT.md` §4.3 |
|            | 构建     | tsc project references            | 契约包多、依赖浅,不需要 bundler                                                                  |
|            | 测试     | Vitest                            | 上游同款,快照模式可照抄                                                                          |
| Supervisor | 语言     | 一期 TypeScript,二期评估 Go       | 一期只管子进程;真要管容器池再换                                                                  |
|            | 隔离     | 子进程 / 容器                     | 见 §2.4                                                                                          |
| API        | 框架     | Hono                              | 轻、Web 标准、可跑 Node 与边缘                                                                   |
|            | 契约     | OpenAPI 3.1 + Zod                 | 单一事实源,SDK 由契约生成                                                                        |
|            | 流式     | SSE 为主,WS 备选                  | SSE 穿透代理更稳,移动端更省电                                                                    |
|            | SDK      | TS / Python 首发                  | 由 OpenAPI 生成,不手写                                                                           |
| 控制面     | 服务     | TypeScript + Hono                 | 与主栈一致,减少心智负担                                                                          |
|            | 数据库   | Postgres                          | 计费与审计需要事务与长保留                                                                       |
|            | ORM      | Drizzle                           | 迁移可读,类型直出                                                                                |
|            | 前端     | React + Vite + TanStack Query     | 与上游 `ui-*` 同技术栈,组件可互通                                                                |
| 移动端     | —        | 生成的 SDK + 各端原生             | 不做跨端框架选型绑定                                                                             |
| 通用       | 包管理   | pnpm 11 workspace                 | 上游同款                                                                                         |
|            | 可观测   | OpenTelemetry                     | 上游已有 `session-telemetry-otel`,直接对接                                                       |

**有意不选**:Rust(基础设施项目里是贡献者门槛)、ORM 重方案、跨端框架、bundler。

### 2.2 核心机制:principal 传播

整套架构成立与否,卡在上游一个 API 上——`ctx.isolate(name, label?)` 返回拥有独立服务槽位的子上下文,相同 label 的调用合并作用域。

```ts
const principal = await ctx.auth.verify(bearerToken) // 边缘认证
const sessionCtx = withPrincipal(ctx, principal) // isolate 出子作用域
// 此下所有插件按 principal 解析，消费方零感知
```

配合上游另一条明文设计——**凭据每次操作解析一次、绝不跨操作缓存**——重新作用域的上下文能在下一次模型请求立刻生效,无需重启任何插件。

**这两条是整个项目的技术前提。M0 必须先验证。**

### 2.3 上游适配纪律

```
packages/**          仅依赖上游「契约包」的公开类型
adapters/dsh-0.1.0/  ★ 唯一允许 import 上游内部实现的目录
```

上游破坏性变更全部撞在 adapters 一层,升级 diff 永远收敛在一个目录。CI 加一条 lint 规则强制这个边界——现在写进去是零成本,事后补是重构。

### 2.4 隔离模型

Harness agent 能执行 shell、读写文件系统。这决定了隔离级别不是配置偏好,是安全等级。

| 级别     | 形态                                | 适用                   | 代价                                    |
| -------- | ----------------------------------- | ---------------------- | --------------------------------------- |
| **逻辑** | 单进程,per-session principal 作用域 | 互相信任的用户(团队内) | 提示词注入、恶意 MCP、污染 skill 可越界 |
| **进程** | 一 principal 一 dsh 进程            | 跨信任边界             | 内存开销,需要进程池                     |
| **容器** | 进程 + OS 沙箱                      | 多租户 SaaS            | 冷启动延迟,运维复杂度                   |

进程级隔离顺带解决**上游 stdio SDK 协议**没有 cancel 与 session-close 方法的问题——终止进程即是取消。

> ⚠️ **这条只适用于走 SDK 协议的消费方。** V0.2.0 Session 0 实测:**进程内**的 `Agent` 接口有显式的 `cancel(cause, options)`,`AgentHandle.dispose()` 亦然,两者都真的截断输出。DSHWAR 网关走进程内驱动,因此**不受此限**。见 `docs/FEASIBILITY-REPORT-V2.md` §4.1。
>
> Supervisor 的**第一个**理由(跨信任边界的安全隔离)完全不受影响,它仍在 V0.4.0。把「取消」列为它的动机会让人误以为 V0.2.0 的网关做不了取消,从而在错误的时间提前一个五周的组件。

> README 与文档必须显著声明:逻辑隔离仅适用于互相信任的用户。宁可劝退采用者,不要让他们从事故中学会。

### 2.5 API 契约版本策略

| 规则       | 内容                                               |
| ---------- | -------------------------------------------------- |
| 路径版本   | `/v1/...`,破坏性变更升大版,双版本并行不少于 6 个月 |
| 与上游解耦 | DSHWAR API v1 的稳定性承诺**不依赖** dsh 版本      |
| 单一事实源 | Zod schema → OpenAPI → SDK,不手写客户端            |
| 契约测试   | 每个上游接触点录制/回放,上游改接口 CI 立刻红       |

### 2.6 跟版机制

- 版本精确锁到 patch,**绝不用 `^`**;运行时校验实际版本,不匹配拒绝启动并给出可读提示
- Renovate 盯 `@deepseek-ai/dsh-*`,新版本自动开 PR
- 升级流程:PR → 契约测试跑红 → **只改 `adapters/`** → 绿了合并
- 目标:**上游小版本 48 小时内跟上**
- README 维护 DSHWAR × dsh 版本兼容矩阵
- 双轨:`stable` 跟已验证版本,`edge` 跟上游最新,社区在 edge 帮忙踩雷

---

## 3. 仓库结构

采用**双仓库**,不是单体。

### 3.1 `dshwar/` — 开源主仓(MIT)

```
packages/
  principal/                  唯一的新概念
  auth/                       契约：verify(token) → Principal
  auth-static/                配置声明的 principal（开发与测试）
  auth-jwt/                   自签 JWT / JWKS
  auth-oidc/                  Keycloak / Authentik / Logto / Auth0
  credentials-multiuser/      per-principal 凭据 + 网关短时效 token 遮蔽
  fs-tenant/                  工作区根按租户钉死 ★ 隔离的真实边界
  storage-scoped/             租户前缀键
  session-tenant/             会话归属与查询过滤
  memory/                     自有契约 + 薄实现（待上游引擎出现后换包装）
  model-router/               模型准入、预算降级、按 principal 限流
  metering/                   token 归属、成本核算、用量查询
  policy/                     工具 / 模型 / 配额授权
  audit/                      仅追加操作轨迹
  supervisor/                 进程编排与隔离级别
  billing/                    契约：计量 → 计费 → 出账
  billing-local/              开源实现：只记账不收款
  subject/                    身份镜像契约 ★ CMS 对接的落点
  scim-server/                SCIM 2.0 服务端（子集）
  webhooks/                   出站事件投递
gateway/                      API 平面服务
sdk/
  typescript/  python/        由 OpenAPI 生成
adapters/
  dsh-0.1.0/                  ★ 唯一允许触碰上游内部的目录
profiles/
  single-user.yml             ★ 对照基线，行为须与原生 dsh 一致
  team.yml                    JWT + SQLite，逻辑隔离
  enterprise.yml              OIDC + Postgres + 进程隔离
examples/
  minimal-server/             两用户两 key，三十行证明论点
docs/                         DECISIONS/ 决策记录 · FEASIBILITY-REPORT.md（Session 0 产出）
```

### 3.2 `dshwar-console/` — 控制平面(MIT,独立仓)

```
api/          租户 · 成员 · 订阅 · 配额策略 · 用量 · 审计查询
web/          运营后台 React 应用
db/           Postgres schema 与迁移
```

独立的原因:技术栈、发版节奏、数据库、部署形态都与运行时插件不是一回事。

### 3.3 闭源部分

只有两块:

- `billing-hosted` — Stripe / 微信支付 / 支付宝的具体接入
- DSHWAR Cloud — 托管服务本体

**开源用户拿到的是可用的完整基座**(`billing-local` 能记账、能出用量报表,自建者够用),商业客户买的是省掉自建的时间。这条线和 `auth` 的处理手法一致——开源契约,闭源托管实现。

---

## 4. 实施路线

### 4.1 里程碑

| 阶段                | 周期 | 交付                                                                               | 验收标准                                                                              |
| ------------------- | ---- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| **M0 可行性**       | 3 天 | principal 传播验证                                                                 | 两用户在同一运行时解析到各自的 key,零消费方改动                                       |
| **M1 运行时 MVP**   | 3 周 | principal / auth / credentials / fs-tenant / storage-scoped + adapters + 契约测试  | `single-user.yml` 与 `team.yml` 单用户行为一致;**开源发布**                           |
| **M2 API 平面**     | 4 周 | OpenAPI v1 + Gateway + 会话路由 + TS/Python SDK + Admin API 的 subjects/usage 端点 | 第三方仅凭 SDK 完成一次完整会话,不接触 dsh；Appsmith/Refine 可直接吃 OpenAPI 生成后台 |
| **M2.5 身份互操作** | 2 周 | Subject Mirror + SCIM 2.0 子集 + 租户映射 + Webhook 骨架                           | Keycloak 经 SCIM 推入两用户，其一在 IdP 侧停用后下次请求即被拒，全程零定制代码        |
| **M3 计量与治理**   | 3 周 | metering + policy + model-router + 用量 API                                        | token 按 principal 准确归属,超配额正确拒绝                                            |
| **M4 控制平面**     | 5 周 | 租户 / 成员 / 订阅 / 运营后台                                                      | 管理员可在后台完成开户到停用全流程                                                    |
| **M5 支付**         | 3 周 | billing 契约 + local + 首个 hosted 实现                                            | 用量→账单→收款闭环跑通                                                                |
| **M6 端**           | 持续 | 移动端 SDK + 对话前端                                                              | iOS/Android 各一个可运行示例                                                          |

累计约 **5.5 个月**到可售基座。记忆治理与 supervisor 进程隔离插在 M3 之后,视上游动向调整。

### 4.2 关键路径

```
M0 ──▶ M1 ──▶ M2 ──┬──▶ M2.5 ──▶ M4 ──▶ M5
                    ├──▶ M3 ─────────────┘
                    └──▶ M6
```

**M2 是总闸**。API 契约没定,移动端、控制面、第三方接入全部堵住。M1 完成后应优先 M2 而非继续加运行时包。

### 4.3 人力

| 阶段  | 配置            |
| ----- | --------------- |
| M0–M2 | 1–2 名 TS 后端  |
| M3–M4 | +1 名前端       |
| M5 起 | +支付与合规支持 |

---

## 5. 启动实施:前四周

### 第 1 周 — 证伪优先

**唯一目标:验证 principal 能沿 cordis 作用域正确传播。**

| #   | 任务                                                | 产出                              |
| --- | --------------------------------------------------- | --------------------------------- |
| 1   | 按 `examples/jsonrpc-demo` 的 `cordis.yml` 跑通 dsh | 本机可运行的运行时                |
| 2   | 跑通 `examples/minimal-server`                      | 两用户各自解析到自己的 key        |
| 3   | 验证凭据不跨操作缓存                                | 换绑 principal 后下次请求立刻生效 |
| 4   | 验证 `ctx.isolate` 的兄弟作用域互不可见             | 并发两 principal 无串号           |

> **止损点**:任务 2 或 4 不通过,说明 cordis 作用域机制与文档不符,§2.2 的技术前提不成立,整套架构需要改为进程级隔离优先。**这个验证只值三天,但它决定后面五个月。**

### 第 2 周 — 骨架与纪律

- 建仓、CI(Node 22 / 24 矩阵)、Renovate、lint 规则强制 adapters 边界
- `adapters/dsh-0.1.0/` 落地,补齐契约测试骨架
- `single-user.yml` 对照基线跑通——这是"只加隔离不改语义"的证明

### 第 3 周 — 隔离的真实边界

- **`fs-tenant`**:工作区根钉死 `{root}/{tenantId}/{userId}`,路径逃逸在此拦截
- `storage-scoped`:先读上游 `storage-domain`,大概率可直接复用
- `auth-jwt`(jose + JWKS)

> `credentials` 解决"用谁的钱",`fs` 解决"能看谁的数据"。后者才是隔离的实际边界。

### 第 4 周 — 开源发布

- 文档:README、隔离模型警告、兼容矩阵、贡献指南
- 商标声明:非官方、指名性使用
- 发布 npm + GitHub,README 首屏回答"和已有的 Electron 封装有什么不同"——**那个做的是打包,DSHWAR 做的是平台**

**不要等完美再开源。** 先发优势建立在"第一个能用的"上,不是"最好的"上。

---

## 6. 风险登记

| #   | 风险                        | 影响   | 应对                                                                          |
| --- | --------------------------- | ------ | ----------------------------------------------------------------------------- |
| 1   | cordis 作用域不支持所需传播 | 架构级 | M0 三天证伪;退路为进程级隔离优先                                              |
| 2   | 上游破坏性变更              | 高频   | 精确锁版 + adapters 单点 + 契约测试 + 48h 跟版                                |
| 3   | 上游自建多租户              | 生存级 | 概率低(本地单用户定位);真发生则转型为其治理层                                 |
| 4   | 逻辑隔离被击穿              | 声誉   | 文档显著警告 + 默认推进程隔离 + 不为便利放宽                                  |
| 5   | 商标争议                    | 法务   | 项目名不含 "DeepSeek";"DSH" 为其 CLI 名,风险低于品牌名但非零,发布前过一次法务 |
| 6   | 范围失控                    | 交付   | 六项能力串行不并行;M2 前不加新运行时包                                        |
| 7   | 记忆 / 多模型与上游撞车     | 返工   | 只做治理层,能力层等上游                                                       |
| 8   | 开源与商业边界模糊          | 商业化 | 闭源仅限 billing-hosted 与托管服务,其余全开                                   |

---

## 7. 立即可做的决定清单

发布前必须定,越早越便宜:

1. **控制平面双仓** — 现在分开是零成本,以后拆是重构
2. **adapters 边界的 lint 规则** — 同上
3. **API 版本策略写进 README** — 采用者第一个问的问题
4. **开源/闭源分界公开** — 藏着会失去信任,明说反而加分
5. **记忆走"自有契约 + 薄实现"** — 决定是否要写一套记忆引擎,直接影响三个月工作量
6. **项目名过法务** — 在有用户之前改名成本最低
