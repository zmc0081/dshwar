# @dshwar/sdk-swift

Swift 模型 —— **由 OpenAPI 契约生成,不手写**。

⚠️ **本包只有模型,没有客户端**。客户端与可运行示例在 V0.9.5,前置是定下分发渠道。重新生成:`pnpm --filter @dshwar/sdk-swift generate`。

**依赖**:无运行时依赖

**完整文档**:仓库 README 的「SDK 快速上手」一节 —— <https://github.com/zmc0081/dshwar#readme>

> 这份 README 刻意只有几行。完整说明在仓库 README,重复一份只会分家 ——
> 而分家的那一刻是静默的。

MIT。
