# @dshwar/console-contract

控制平面契约:租户 / 成员 / 角色 / 配额 / 用量 / 审计查询的共享类型。

⚠️ 与 `/v1` 运行时契约**分开版本化**,且**不在 OpenAPI 冻结检查的基线里** —— 它是 TS 类型契约,没有 `openapi.json`。

**依赖**:无运行时依赖

**完整文档**:仓库 README 的「API 平面」一节 —— <https://github.com/zmc0081/dshwar#readme>

> 这份 README 刻意只有几行。完整说明在仓库 README,重复一份只会分家 ——
> 而分家的那一刻是静默的。

MIT。
