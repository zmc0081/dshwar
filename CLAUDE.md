# CLAUDE.md — DSHWAR 项目约束(权威源)

> **本文件由 Claude Code 每次启动自动加载,是约束的唯一权威源。** 任一违反 = PR 阻塞。
> 当前版本(正在开发): **V0.9.0**
> 仓库: `dshwar`(开源主仓,MIT) · `dshwar-console`(控制平面,M4 启用)

---

## 零、文档地图(按需读取,不自动加载)

以下文件**不会**随本文件自动进入上下文,需要时按下表主动读取。
⚠️ **禁止在本文件中用 `@` 语法引入它们**——`@` 导入在启动时就会把整个文件塞进上下文,
`SESSION_TASKS.md` 会随版本增长到十几万字符,导入即等于每次会话烧掉全部预算。

| 文件                                               | 什么时候读                                                                                         |
| -------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| `SESSION_TASKS.md`                                 | **每个 Session 开工必读**——找到当前版本块与本次 Session 的任务详情                                 |
| `ARCHITECTURE.md`                                  | 涉及跨平面设计、路线调整、新增包立项时                                                             |
| `IDENTITY-INTEROP.md`                              | 涉及认证、SCIM、租户映射、CMS / 后台对接时                                                         |
| `KICKOFF.md`                                       | 仅首次搭建环境与仓库时                                                                             |
| `SESSION_TASKS_HISTORY.md`                         | **仅**追溯已发布版本的具体实现时;日常开发不要读                                                    |
| `README.md`                                        | 修改对外表述、兼容矩阵、开源边界声明时                                                             |
| `docs/DECISIONS/unverified-plausible-causation.md` | **当一个因果解释听起来特别顺的时候**——收录三个已写进文档、指导过决策、后来被实测推翻的「顺理成章」 |

---

## 一、项目是什么

DSHWAR 是 **DeepSeek Harness 之上的 ToB 产品基座**。上游是本地单用户的 Agent 运行时;DSHWAR 补齐商业应用需要的用户体系、隔离、计量、计费、运营与多端接入。

### 一句话边界(所有决策的裁决标准)

> **上游做能力,DSHWAR 做归属、隔离、配额、计费、审计。**

新增任何包之前先回答:属于"能力"还是"治理"?

- 属于**治理** → 放心做。单用户场景不需要治理,上游永远不会碰。
- 属于**能力** → 先确认上游三个月内不会做。记忆(`context/` `compaction/` `spill/`)、模型编排(`llm/` `subagent/` `workflow/`)、对话 UI(`client/ui-*`)都是上游地盘,**只做治理层,不造引擎**。

### 三平面

```
控制平面  租户/订阅/支付/配额/审计/后台     独立仓库、独立数据库、与 dsh 零耦合
API 平面  运行时 API + 管理 API + SCIM     ★ 护城河，契约由 DSHWAR 定义与版本化
运行时平面 principal/credentials/fs/storage  cordis 插件，跟随 dsh 版本
──────────────────────────────────────
DeepSeek Harness（npm 依赖，精确锁版）
```

---

## 二、硬规则(9 条,PR 阻塞级)

1. **禁止 fork / patch 上游**。从 npm 消费 `@deepseek-ai/dsh-*`。需要改上游才能实现 → 提 issue,不建 patch 目录。
2. **只有 `adapters/dsh-<version>/` 允许 import 上游内部实现**。`packages/**` 与 `gateway/**` 仅可依赖上游契约包的公开导出,禁止深链 `/lib/` `/src/` 路径。
3. **上游依赖精确锁版**,禁止 `^` 与 `~`。运行时校验实际版本,不匹配拒绝启动并给出可读提示。
4. **禁止存储密码、禁止签发身份令牌、禁止实现注册流程**。DSHWAR 是身份消费者,不是提供者。凡出现 `bcrypt` / `argon2` / `password` 字段即违规。
5. **凭据端点只暴露 `describe` 语义**(configured / source / writable),**永不返回值**。这是上游 `dsh-credentials` 的既有约束,必须原样传递到 Admin API。
6. **缺失 principal 时一律 fail closed**。匿名 principal 解析不到任何凭据,不得回退到默认值或共享 key。
7. **租户映射 fallback 默认 `reject`**。映射不出租户的用户宁可拒绝登录——落进默认租户意味着 A 公司的人能看到 B 公司的工作区。改为 `fixed` 需在 PR 描述中显式说明理由。
8. **不改上游语义**。`profiles/single-user.yml` 与多用户 profile 在单用户场景下行为必须一致,契约测试强制。
9. **开源分发的构建产物不得包含任何闭源组件**——`billing-hosted` 等闭源部分必须是独立构建产物。这既是 open-core 的边界,也是 SignPath Foundation 免费签名的资格条件(不得含维护者或关联组织发布的专有代码)

### PR 自查(grep 必须全为 0 / 全绿)

```bash
# 2. 深链上游内部实现
grep -rE "@deepseek-ai/dsh-[a-z-]+/(lib|src|dist)/" packages/ gateway/ adapters/../  → 0

# 3. 上游依赖锁版
grep -rE '"@deepseek-ai/[a-z-]+": *"[\^~]' packages/*/package.json gateway/package.json → 0

# 4. 密码体系
grep -rniE "bcrypt|argon2|scrypt|passwordHash|password_hash" packages/ gateway/ --include=*.ts → 0

# 5/6. 凭据泄漏与默认回退
grep -rnE "resolve\(.*\)\.value" gateway/ --include=*.ts                      → 0（Admin API 不得取值）
grep -rn "ANONYMOUS" packages/*/src --include=*.ts                            → 仅 principal 包

# 配置只经 profile 注入，不散落 env 读取
grep -rn "process\.env" packages/ --include=*.ts                              → 0

# 9. 开源构建产物不含闭源组件
node scripts/check-oss-purity.mjs   # 开源构建产物不含闭源组件（硬规则 9）        → 0

# 门禁
pnpm typecheck                                                                → clean
pnpm typecheck:test   # 测试文件走 tsconfig.test.json,不跑这条它们不被检查      → clean
pnpm test && pnpm test:contract                                               → green
pnpm eslint . --max-warnings 0                                                → clean
```

---

## 三、文档瘦身与归档(强制)

**目的**:`SESSION_TASKS.md` 必须始终保持在 **Claude Code 单文件读取上限(150,000 字符)** 以内。超限时 Claude Code 读不全任务书,会基于残缺上下文开发,**且不会主动告知哪部分被截断**。

**触发时机**:每次版本**开发完成后**(全部 Session 标 ✅ 时)**立即执行**。

> ⚠️ **2026-08-16 判据修正。** 原文写的是「版本发布后」。改为「开发完成后」的理由:
> 本节的目的是保住主文件在读取上限内,而**发布是对外动作,与主文件涨不涨无关**。
> 按原判据执行的实际后果是四个版本全部开发完成却一个都不能压缩,主文件一路逼近上限。

**压缩规则**

| 类别                               | 处理       |
| ---------------------------------- | ---------- |
| 版本标题                           | 保留       |
| 简介引用段                         | 保留       |
| 交付内容表(改了什么)               | 保留       |
| 包含的 Session 标题列表            | 保留       |
| 核心改进要点                       | 保留       |
| **Session prompt 代码块**          | 删除并归档 |
| **实现步骤 / 接口规格 / 契约细节** | 删除并归档 |
| **验证动作 / 测试清单**            | 删除并归档 |
| **git 命令**                       | 删除并归档 |

**一句话标准:记录「改了什么」,不记录「怎么改的」。**

压缩后的版本块末尾追加:`> 实现细节见 SESSION_TASKS_HISTORY.md`

**归档规则**

- 被删内容**完整原样**追加到 `SESSION_TASKS_HISTORY.md` **开头**(保持从新到旧)
- 归档不做任何删减,不受体积限制
- **开发中的版本永不压缩**,保留完整任务详情供开发使用;
  已完成但未发布的版本**可以**压缩 —— 其 prompt 不再被任何人执行

**校验**(三条,缺一不可):

1. 压缩后主文件字符数 < 150,000。**超过 100,000(上限 2/3)即应准备压缩**。
2. **压缩前的每个 `###` 小节标题,必须能在主文件或归档里找到。** 少一个就是丢了内容。
3. **主文件里不得残留 Session prompt。** 判据:`grep -c '^读取 CLAUDE.md' SESSION_TASKS.md` → 0。

> ⚠️ **第 2、3 条是 V0.4.6 补的,起因是压缩脚本自己出过 bug。** 那次切分器
> 把后来插入的、以 `---` 结尾的引用段当成小节结束,于是漏下两段孤儿 prompt ——
> 而字符数看起来完全正常。**这类工具 bug 会重复发生,校验比修复值钱**:
> 修复只解决这一次,校验解决下一次。

### ★ 改这个文件一律用 `scripts/session-tasks.mjs`,不要写临时的 `node -e`

```bash
node scripts/session-tasks.mjs status --version 0.6.5 --session 2 --mark ✅
node scripts/session-tasks.mjs archive --version 0.6.5 --summary-file <path>
node scripts/session-tasks.mjs check      # 三条校验,已进 check:all
```

**「校验比修复值钱」这句话是对的,但它不是「只加校验」的许可。**
校验已经抓住了五次同一形状,而五次都还在发生 —— 说明工具本身没修。
V0.6.5 收官把两个根因都根除了,它们**是两回事**,不要混:

| #   | 根因                                         | 症状                                        | 根除方式                                                      |
| --- | -------------------------------------------- | ------------------------------------------- | ------------------------------------------------------------- |
| 1   | **shell 把反引号当命令替换**(5 次)           | 单元格变**空**,而整条命令**退出码 0**       | 内容一律从**文件**读(`--summary-file`),命令行只传路径与短标识 |
| 2   | 正则匹配整行,Prettier 一重排列宽就失配(2 次) | `replace` 安静返回原文,状态**退回上一个值** | 定位改走 `lib/md-table.mjs` 的**按列内容匹配**;失配**抛错**   |

⚠️ **根因 1 不是 Markdown 问题,是 shell 引号问题。** 判据很硬:

```bash
node -e "console.log('| `@dshwar/x` |')"
# bash: @dshwar/x: No such file or directory   ← 只到 stderr
# | |                                          ← 内容没了
# echo $? → 0                                  ← 静默
```

**heredoc 同样会吃**(实测 `<<'EOF'` 里的 `\\b` 变成 `\b` = 退格符,
正则于是永不匹配)。所以规则不是「记得转义」,而是**内容不进 shell 字符串**:
写文件用 Write 工具,读文件用 `--xxx-file` 参数。

⚠️ 工具改完表格不重排列宽 —— 跑一次 `pnpm format` 收口即可。
忘了的话 `format:check` 会红,**那是响亮的失败,不是静默的**。

> ⚠️ **单位是字符,不是字节。** 中文在 UTF-8 下一个字符占 3 字节,两者差约 1.6 倍 ——
> 按字节判断会提前一大截触发压缩。量的时候用:
>
> ```bash
> node -e "console.log(require('fs').readFileSync('SESSION_TASKS.md','utf8').length)"
> ```
>
> (`wc -c` 给的是字节,`ls -l` 也是。V0.4.5 Session 4 实测:78,882 字节 = 48,455 字符。)

**Session 状态图例(统一)**:✅ 已完成 · 🔄 进行中 · ⬜ 未开始 · 🟠 代码就绪待外部资源
已发布版本的 Session 一律标 ✅;开发中版本每完成一个即更新,并维护块头部的「Session 状态」小结表。

---

## 四、版本号统一更新(强制)

所有 `@dshwar/*` 包**统一版本号**(changesets fixed 模式)。每次发版以下位置必须一致,任一不一致 = 发布阻塞:

1. root `package.json` 的 `version`
2. 各 workspace 包的 `version`(由 changesets 统一提升)
3. `CLAUDE.md` 顶部「当前版本」
4. `SESSION_TASKS.md` 头部「当前版本(正在开发)」
5. `README.md` 兼容矩阵中的 DSHWAR 版本行
6. `gateway` 的 OpenAPI `info.version`

**开发版本号即时同步(强制)**:新版本规划确立后、第一个 Session 开工前,必须先把上述位置更新为**正在开发的版本号**。效果:开发环境构建产物版本号 = 正在开发版本号 = 最终发布版本号,发布时无需再改。

**changeset 收敛(强制,与上一条配套)**:提升开发版本号时,把 `.changeset/*.md` 的内容**并入 `CHANGELOG.md` 并删除**。

> 为什么:预标版本号与 changesets 的 bump 模型天然冲突 —— 任何待发布的 `minor` 变更集都会把预标的版本再推一级,与「发布时无需再改」直接矛盾。
>
> 这不是绕过工具。**changesets 记录的是「发布之间」的增量,而首发之前不存在「之间」**;变更集描述的是一个从未发布过的东西的演进过程,那属于 CHANGELOG 的「初版包含什么」。
>
> **首次发布之后恢复正常流程**:那时有了真实的「上一版」作参照,预标与 bump 不再冲突。
>
> ⚠️ 真走 `changeset version` 时注意:它**不提升 root `package.json`**(root 是 `private`),而 `check-version` 拿 root 当基准 —— 必须手工同步。见 `docs/RELEASE-CHECKLIST.md`。

---

## 五、上游跟版(强制)

- Renovate 盯 `@deepseek-ai/dsh-*`,新版本自动开 PR
- 升级流程:Renovate PR → `pnpm test:contract` 跑红 → **只改 `adapters/`** → 绿了合并
- 目标:上游小版本 **48 小时内**跟上
- `README.md` 维护 DSHWAR × dsh 兼容矩阵,每次跟版更新
- 双轨:`stable` 跟已验证版本,`edge` 跟上游最新
- ⚠️ 上游 npm registry 版本号与 monorepo 根版本号**不一致**,**一律以 registry 为准**
- ⚠️ **上游子包的 `dist-tags.latest` 是坏的**:除 `@deepseek-ai/dsh` 本体外,全部子包的 `latest` 停留在 `0.0.1-rc.1`,而实际已发布到 `0.1.0-rc.6`。**Renovate 不得依赖 `latest` 标签**,否则永远不开 PR。实测见 `docs/FEASIBILITY-REPORT.md` §4.3
  - ✅ **但有 `next` 标签,且指向正确版本**(V0.4.6 实测):`dsh-llm` 与 `dsh-llm-deepseek` 的 `dist-tags` 都是 `{ latest: 0.0.1-rc.1, next: 0.1.0-rc.6 }`。**Renovate 可以直接跟 `next`**,比按版本号排序省事
  - ⚠️ 各子包**在同一条版本线上**,不是「版本线不统一」—— 是同一条线加同一个坏标签。不要因为看到 `0.0.1-rc.1` 就以为某个包落后了
- 当前锁定版本:**`0.1.0-rc.6`**(Session 0 验证基线),适配目录 `adapters/dsh-0.1.0/`

---

## 六、代码规范

**TypeScript**:strict + `exactOptionalPropertyTypes` + `noUncheckedIndexedAccess`;ESLint + Prettier;禁止 `any`(必要时 `unknown` + 收窄);公开导出必须有 TSDoc,写**为什么**而非重述签名。

**契约包**:抽象类继承 cordis `Service`,通过 `declare module` 做模块增强挂到 `Context`;实现包命名 `<contract>-<impl>`,与上游 `fs-local` / `storage-sqlite` 的惯例一致。

**测试**:Vitest。每个上游接触点必须有契约测试(录制/回放),上游改接口即刻跑红。

### ★ 元规则:每新增一个验证机制,必须回答「谁验证它?」

> **答不上来不算做完。** 这一条与硬规则同级 —— 它管的是所有硬规则赖以生效的那一层。

**为什么需要一条元规则,而不是靠细心。** 同一个形状已经出现**六次**,一次比一次靠里
—— 而最后两次是**主动去扫**才找出来的,不是等它咬人:

| #   | 什么没被验证                                        | 怎么发现的                          | 之前"绿"了多久                        |
| --- | --------------------------------------------------- | ----------------------------------- | ------------------------------------- |
| 1   | **测试文件**不被 tsc 检查                           | 一次会话里连踩三个编译期可见的错误  | 全仓 40+ 测试文件,从 V0.1.0 起        |
| 2   | **CI 不跑 `verify:assertions`** —— 盯守卫的那条     | 首次真实 runner 复盘                | 从 V0.4.6 加进 `check:all` 起         |
| 3   | **根 `scripts/` 不被类型检查** —— 守卫脚本自己      | 修 #2 时顺手查出                    | 两个版本                              |
| 4   | **变异验证自己没生效** —— 探针改 src,测试读 dist    | V0.6.5 一次偶然重建后才红           | 探针 4 的 gateway 目标,两个版本       |
| 5   | **CI 的 `terminal-contract` job 只 `echo`** —— 恒绿 | V0.6.5 收官主动扫「还有没有第二个」 | 从 V0.4.5 加进 CI 起                  |
| 6   | **`process-cost` 缺平台阈值就 exit 0** —— 恒绿      | 同上,同一次扫描                     | 同上;且这是该门禁的**第二条**恒绿路径 |

**六次都是「检查机制自己的那一层没人管」**,而且**前三次都是等它咬人才修**。
第三次是唯一一次在咬人之前就动手的 —— 只因为前两次刚发生,人还记着。
**记着是会过期的,规则不会。**

⚠️ **#6 值得单独看**:同一个脚本先后有两条互不相关的路径通向恒绿
(`median([])` 返 NaN → 比较恒为 false;平台阈值缺条目 → exit 0)。
**判定型的检查天然有多条退化路径** —— 输入退化、配置退化、比较退化各是一条,
而它们的共同表现是同一个绿勾。给判定型门禁写负向验证时,
不要只验「超标会红」,还要问**「有哪些输入或配置会让它连比较都不做」**。

### ★ 设计准则:第一问不是「这东西在不在」,而是「它坏了会不会被发现」

> **这已是第七次同一转向。** 每次都是先写出一条「查存在性」的规则,
> 用过之后发现它拦不住真实的失效形态,再把判据往后挪一格。
> 上一版这里写的是「与其等第四次」—— **第四次三天后就到了**,见下表第 4 行。
> 所以这个转向不是要「预防」的东西,是设计时的默认起手式。

| #   | 第一版判据(查存在)         | 改成的判据(查失效可见性)                 | 为什么前者不够                                    |
| --- | -------------------------- | ---------------------------------------- | ------------------------------------------------- |
| 1   | 遍历集合前断言**集合非空** | **数真正断言过几次**(出口计数)           | 嵌套 `continue` 能把入口非空的循环全过滤掉        |
| 2   | CI job 有没有**跑命令**    | 它的检查坏掉时**谁会红**(登记制)         | job 可以跑一条真命令,却没人验证那条命令会不会红   |
| 3   | 探针**挂了测试目标**       | **一条探针一个目标**                     | 多目标时红是「或」—— 一个目标的红盖住另一个的空跑 |
| 4   | 组件**能不能装配**         | **出厂到底装了什么**                     | 组件挂得上、单测全绿,而出厂产物根本没挂它         |
| 5   | 分类器**有没有负向验证**   | **每个分类码各一条 + 完备性断言**        | 一条负向验证的绿,会被读成「整个分类器已验证」     |
| 6   | 这一维**有没有**负向验证   | 这一维内部还有没有**未被触及的表达方式** | 打了勾的维度可能只覆盖了自己的一半                |
| 7   | 补上了**哪些漏报**         | 有没有**多报**(留反向对照)               | 按已知缺口列的清单,结构上不含误报                 |

⚠️ **第 4 行是 V0.8.0 实测,代价是三个版本。** `registerWorkspaceRoutes`
从 V0.5.5 起实现完整、测试齐全,而 `server.ts` 从不传 `workbenchRoutes` ——
七条 `/v1/workspaces*` 在真实部署里全部 404,**一道红都没有**。
原因是所有工作台测试**自己手工调 `createGateway` 把它挂上去**:
验的是「挂进去之后行为正确」,不是「出厂真的挂了它」。

同一个盲区还盖着 `/v1/jobs`(契约标 `implemented` 而 handler 根本不存在)
与 billing(`billingRoutes` 同样不传)。**三处各自看像「忘了一行」,
合起来是缺少一道「出厂产物 = 契约承诺」的对照。**

⇒ 落到操作上:**测试构造被测对象的方式本身就是判据的一部分。**
一个测试自己拼装被测系统时,它验的永远是「这么拼能工作」——
而出厂拼法对不对,得由**调用出厂入口**的测试来答。
盯这一层的是 `gateway/test/shipped-assembly.test.ts`(调 `startServer()`,
起真实端口发真实 HTTP),负向验证在 `verify-assertions.mjs` 探针 19。

**共同形状**:存在性是**结构**上的问题,而恒绿是**行为**上的问题。
查结构永远查不到行为 —— 一个结构完整、行为空转的检查,在输出里
与真正通过的检查一模一样。

**落到操作上**:新写一条守卫 / 断言 / CI job 时,先写下这句话的答案 ——

> **「假设它已经坏了,我会从哪里看出来?」**

答案若是「跑一遍看它红不红」,那还不够 —— 那只证明它在**当前仓库状态**下
不报错,是最弱的一种证据。答案要具体到**哪一条负向验证、植入什么违规**。

#### ⚠️ 「谁验证它」两个方向都要问 —— 报错的那一半同样会骗人

这条元规则最初只覆盖了一半:**「它该红的时候会不会红」**。
但工具还有另一半会失效 —— **「它红的时候是不是真的有问题」**。

> **一个报错的工具与一个不报错的工具,同样需要被核对。**

两次实测,都发生在写这条规则的同一轮里:

| 工具                             | 报了什么                 | 真相                                                   |
| -------------------------------- | ------------------------ | ------------------------------------------------------ |
| `session-tasks.mjs check` 第一版 | 三个版本块「缺归档指针」 | **全都有** —— 版本块定位抓到了顶部路线图               |
| 同上第二版                       | 两个版本块仍「缺指针」   | **也有** —— 区间下界按「下一个 `##`」收,被小节标题截断 |

⇒ **假阳性与假阴性同样危险,而且假阳性更能骗到人**:它看起来像是
「工具在干活」,于是人会去改**被误报的那个东西**。第一版若不核对,
就会去给三个本来正确的版本块「补」一个已经存在的指针。

**具体要求**:一个检查**第一次报红**时,先核对那条红对不对,再动手修被报的东西。
核对的成本通常是几分钟,而按误报去改代码的成本是「改坏一个本来正确的东西,
并且以为自己修好了」。

### ★ CI 的每个 job 必须在守卫或探针的计数里有对应项

> **不在计数里 = 没人看着它。** 而一个没人看着的 job 会在面板上占一个绿勾 ——
> **比没有 job 更糟**。

| 判据       | 说明                                                                                                   |
| ---------- | ------------------------------------------------------------------------------------------------------ |
| **实质性** | 去掉准备步骤(checkout / setup / install / build)后,至少还剩一条不是 `echo` / `true` / `:` 的 `run`     |
| **已登记** | job 名必须在 `check-guards.mjs` 的 `CI_JOB_VERIFICATION` 里,填的是**「这个 job 的检查坏掉时,谁会红」** |

判据 2 比判据 1 强得多:**一个 job 可以跑一条真命令,却没人验证那条命令
会不会红**。`measure-process-cost --assert` 在 2026-08-17 之前正是如此 ——
它甚至还有一条「平台没有阈值条目就退出 0」的路径,在 CI 上等于恒绿
(现已由 `--require-threshold` 堵上)。

**谁验证它**:`verify-guards.mjs` 的 26a(只 echo 的 job)、26b(未登记)、
**26c 正向对照**(合规的 job 必须被放行 —— 否则一个「见到 job 就红」的
实现也能通过前两条)。

⚠️ **CI 面板不是待办清单。** 「先放个占位 job,以后实现」听起来无害,
实际是给自己发了一个永久有效的绿勾。待办写进 `SESSION_TASKS.md` 或
`docs/RELEASE-CHECKLIST.md` —— 那里没人会把它误读成「已经在检查了」。

### ★ 变异验证:改完必须同步 `dist`,否则「没变红」是两种意思

> **这是 #4 的规则化。** 一条报「通过」的探针,可能从来没测到任何东西。

各包 `package.json` 的 `main` 指向 `dist/`,而 Vitest **没有**把 `@dshwar/*`
指回 `src` 的 alias。于是测试怎么 import 决定了变异看不看得见:

| 测试怎么 import         | 变异改 src 后         |
| ----------------------- | --------------------- |
| `../src/index.ts`(同包) | ✅ 立刻看得到         |
| `@dshwar/<pkg>`(跨包)   | ❌ **读 dist,看不到** |

而 `check:all` 里 `typecheck`(`tsc -b`)跑在探针**之前** —— dist 是用
**未变异**的源码构建的。所以「改坏了,测试还是绿」有两种含义,
**它们在输出里长得一模一样**:

1. 断言弱(真问题,该加强断言)
2. **根本没测到改动**(工具问题,加强断言等于基于误判改代码)

**规则**:变异后跑测试,一律走 `scripts/lib/mutate.mjs` 的
`runTestsUnderMutation()` —— 它变异后同步 dist、跑完还原、还原后再重建
(不把变异过的 dist 留给下一条探针)。**不要自己拼 `vitest.mjs` 的路径。**

⚠️ **一条探针挂多个测试目标时,红是「或」** —— 一个目标的红会盖住另一个
目标的空跑,而整条探针照样报「通过」。探针 4 就是这么在两个版本里
挂着一个从未验证过任何东西的 gateway 目标。**一条探针一个目标**;
真要多个,分成多条。

**谁验证它**:`check-guards.mjs` 的「变异后跑测试走受控通路」守卫 ——
`scripts/` 下除 `lib/mutate.mjs` 外出现 vitest 路径字面量即红,
负向验证在 `verify-guards.mjs` 的 25a/25b(含正向对照:
规则不是「见到 vitest 就红」)。

**这一条要在立项时回答,不是在收工时补。** 具体到三类:

| 新增了什么                                      | 必须同时给出                                                                   |
| ----------------------------------------------- | ------------------------------------------------------------------------------ |
| 一条**守卫**                                    | `verify-guards.mjs` 里的负向验证 —— 植入违规,确认它真的红                      |
| 一条**断言 / 测试**                             | 若它守的是核心不变式,给 `verify-assertions.mjs` 一条探针:弄坏实现,确认它真的红 |
| 一个**检查项目**(tsconfig / lint 配置 / CI job) | 回答「它要是根本没跑,我怎么会知道?」—— 通常是再加一条守卫盯着它                |

⚠️ **「跑绿了」不是答案。** 一条永远绿的检查与没有检查等价,但更危险:它让人以为有覆盖。
`tsc -b` 对 `include: []` 的空项目**安静地成功** —— 那种绿与「检查过且通过」在输出上一模一样。

### ★ 分类器有几个分类码,负向验证就要有几条 —— 并且要有一条**完备性断言**

> 与「一条探针一个目标」并列,方向相反:
> 那条管的是**多目标掩盖空跑**,这条管的是**单维掩盖未覆盖的维度**。

**判据**:一个检查若沿多个**互相独立**的维度分类(维度的判据是「一个坏了另一个
不会跟着坏」),那么

1. **每个维度各有一条负向验证** —— 各自植入一次真实违规,确认它真的红;
2. **另有一条完备性断言**,把「维度清单」与「负向验证的表」钉死 ——
   **表里少一维就红**。

第 2 条是关键。少了它,第 1 条会随时间退化成「写到哪算哪」,
而退化的过程**完全不可见**:分类码从 14 个长到 18 个,负向验证一条没跟着长,
门禁始终显示为绿。

⚠️ **完备性断言必须从源头现取清单,不许抄一份。** 抄一份就等于给自己留了
「两边不同步而没人知道」的第二个洞 —— 那正是这条规则要堵的东西。
TS 的类型联合在运行时不存在,所以清单要写成 `as const` 数组再派生类型
(见 `packages/api-contract/src/freeze.ts` 的 `CONTRACT_CHANGE_CODES`)。

#### 它是怎么被抓到的(V0.8.0,「谁验证它」第七次)

`scripts/check-contract.mjs` 输出的那句「**破坏性 N 处**」,
全项目当它是兼容性的权威判定。而它的负向验证只有一条:
**删掉一条 path** —— 八个 breaking 分类码里的一个。

逐条植入 15 种真实的破坏性变更实测,**8 种漏报**:

| 漏报的变更                                  | 为什么                                    |
| ------------------------------------------- | ----------------------------------------- |
| 响应体 `$ref` 换成不相干的类型              | `diffContract` **根本不遍历 `responses`** |
| 删掉已声明的响应码(200 / 404)               | 同上                                      |
| 请求体 schema 换掉                          | 同上,`requestBody` 也不遍历               |
| **`components.schemas` 内部**的 `$ref` 换掉 | `JsonSchema` 接口里**没有 `$ref` 字段**   |
| `anyOf` 分支改类型 / 收窄                   | `anyOf`/`oneOf`/`allOf` 声明了却从不比对  |
| operation 的 `security` 改掉                | 不在遍历范围内                            |

最刺眼的是第四行:`components.schemas` 恰恰是大家默认「**已经覆盖了**」的那一维,
而本仓的契约用 `$ref` 表达一切跨类型引用 —— 于是那一维只覆盖了它自己的一半。

**谁验证它**:`verify-guards.mjs` 的 `8/9 [<code>]` 一族 —— 逐码植入一次真实
变更、跑一次真实门禁、断言退出码与输出里的码;末尾一条
「每个 `ContractChangeCode` 都有一条负向验证(完备性)」。
负向验证过一次:往 `CONTRACT_CHANGE_CODES` 里加一个没有对应条目的码,
完备性断言变红**并点名那个码**。

#### ⚠️ 一个维度「有覆盖」不等于「覆盖完整」

> **判据从「这一维有没有负向验证」改成「这一维内部还有没有未被触及的表达方式」。**

上面那条完备性断言保证的是**每一维都被碰过**,而不是**每一维都被走遍**。
两者的差距可以很大,而且差得毫无痕迹。

**实测**:`components.schemas` 是覆盖清单上**打了勾**的一维 ——
`schema.removed` / `property.removed` / `property.type.changed` /
`enum.value.*` 全在,单测也齐。但它**只覆盖了自己的一半**:
`JsonSchema` 接口里没有 `$ref` 字段,也从不比对 `anyOf`/`oneOf`/`allOf`,
而本仓的契约恰恰用这两样表达**一切**跨类型引用与可空。

于是同一维之内:

| 表达方式                                     | 覆盖到了吗                           |
| -------------------------------------------- | ------------------------------------ |
| `type` / `properties` / `required` / `enum`  | ✅                                   |
| `$ref`(全文档 180 处)                        | ❌ 换掉指向 = 0 处破坏               |
| `anyOf`(24 处,全是可空包装)                  | ❌ 改类型、去掉 null = 0 处破坏      |
| `oneOf`(1 处,`StreamEvent` 的 9 个 SSE 事件) | ❌ **删掉一整个事件类型 = 0 处破坏** |

最后一行值得停一下:**SSE 是这份契约的旗舰面**,而 `StreamEvent` 节点上
既没有 `type` 也没有 `properties` —— `diffSchema` 走完一遍**一个字段都比不到**。
它不是「覆盖得浅」,是**整个类型对比较器不可见**。

**具体要求**:给一维写完负向验证后,再问一次 ——

> **「这一维的数据,在真实文档里一共有几种长法?我碰过其中几种?」**

答法是**去数**,不是去想:遍历真实产物统计各种表达方式的出现次数
(本次就是这么发现 24 个 `anyOf`、1 个 `oneOf`、0 个 `allOf` 的),
然后逐种植入一次。数出来是 0 的那种,在注释里写明**它今天是空的** ——
一个悄悄什么都不判的分支,与没有这个分支等价。

⚠️ **顺带的一条**:兜底分支要判**破坏**,不要放行。
认不出来的结构变化必须停下来让人看一眼 —— 「不认识就放过」正是
这一整节要拦的那种沉默。

#### ⚠️ 补完之后必须**追溯**一遍,而且要逐步走,不能只比首尾

补完分类器,拿它把**历史**重跑一遍 —— 此前那些「破坏性 0 处」不是错的,
是**没被验证过的**;这一遍是把「没验过」变成「验过」。

**逐提交两两比,不要只比首尾。** 累计差异有两个盲区:

1. 中途破坏、后来又改回来的,首尾看不见;
2. **基线之后才新增的路径**,首尾只报 `path.added`,其响应体从不参与比较。

V0.8.0 实测正是撞上第 2 条:累计 V0.2.0 → 当前报 0 处,而逐对走到第 16 步
(V0.5.5 Session 0)报出 8 处 —— `/v1/admin/capacity` 的 200 响应
从内联结构改成了 `$ref: Capacity`。

⚠️ **那 8 处是误报,而追溯正是发现它的方式。** 解引用后两者逐字节相同 ——
「把内联结构提取成具名类型」是纯重构。同一轮里追溯还逼出了第二条豁免:
`planned` 转正时那个 501 消失,是契约先行策略**承诺过**会发生的收缩。
⇒ **新规则的第一次运行必须对着历史跑,不是对着当前跑。**
当前只有一个样本,历史有二十一个。

### ★ 每一轮补覆盖,都要留几条「这一定不能红」的反向对照

> **按已知缺口列的清单,结构上不可能包含误报** ——
> 它照的是「漏了什么」,照不到「多报了什么」。

一轮补覆盖的清单是这么来的:发现漏报 → 列出漏了哪些 → 逐条补上 → 逐条验它会红。
整条链上每一步问的都是**同一个方向**的问题。于是清单里**一条误报也不会有**,
不是因为没有误报,是因为这种列法照不到它。

**实测**:V0.8.0 补契约冻结检查,第一轮 15 例探针补掉 8 个漏报,全绿。
第二轮换个角度重列清单,立刻撞出一处**误报** ——
`diffUnion` 把「联合多了一个分支」判成破坏性,于是**给 SSE 加一个事件类型
会被拦住**,而那正是 V0.4.6 放宽 `enum.value.added` 时点名要允许的演进。
第一轮不可能发现它:那一轮的每一条都在问「这个违规会不会被抓到」。

#### 误报为什么比漏报更该被单独盯

|          | 表现             | 人的反应             |
| -------- | ---------------- | -------------------- |
| 漏报     | 该红没红         | 没人知道,直到出事    |
| **误报** | **不该红却红了** | **学会绕过这条检查** |

绕过的方式很具体:给正确的代码加豁免标记、用 `--base` 换个基线、
或者写一份笼统的 changeset 一起吞掉。**吞过一次,以后每次都会吞** ——
而那一次里混着的真实破坏会跟着一起放行。

⇒ **一条会让人学会忽略它的规则,比一个漏报更贵。**

#### 具体要求

每加一组「植入违规 → 确认变红」,同时加几条**「植入合法写法 → 确认放行」**。
反向对照要照着**真实存在的合法形态**挑,不是构造边角:

| 检查                      | 反向对照挑的是什么                                                                                                |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `check-guards` 的 grep 族 | 带理由的豁免标记 · `describe().configured`(硬规则 5 允许的形态)· `gateway/` 下读 env · `test/` 里构造 `ANONYMOUS` |
| `check-oss-purity`        | 依赖 / import `@dshwar/billing-stripe`(D4 判它开源)· `private` 包依赖闭源组件 · `files: ['dist','README.md']`     |
| `measure-process-cost`    | **真实阈值下不红** —— 阈值不能定得让正常波动就超标                                                                |
| 出厂装配守卫              | 资源级 404 · 可选依赖没配导致的 501 —— 两者都不算「端点没挂」                                                     |

⚠️ **反向对照本身也要负向验证**,否则又是一堆恒绿的断言:
把对应的规则**故意放宽到会误报**,确认那条对照真的红。
V0.8.0 逐条验过七条放宽,全部立刻可见 —— 其中一条(判据退化成
「只看状态码」)一次制造出 **11 条误报**,把每个合法的资源级 404
都报成「端点没挂」。

⚠️ **验证方法有个坑**:若某条对照守的合法形态**真实仓库里今天不存在**,
放宽规则后跑被测脚本仍是绿的 —— 那不代表对照没用,代表**验证时忘了把夹具摆上**。
`31f`(`describe().configured`)就是这样:不带夹具跑,放宽后仍绿;
夹具在场再跑,立刻红。**验反向对照时,夹具必须在场。**

### ★ 验反向对照时,夹具必须在场

反向对照守的是「**这个合法形态不能被误报**」。而那个合法形态在真实仓库里
**可能一个实例都没有** —— 那样把规则放宽,被测脚本照样是绿的,
于是验证退化成恒绿,而它看起来与「验过了」一模一样。

**实例**:`31f` 守的是硬规则 5 允许的形态 `describe(ref).configured`。
把凭据守卫的正则放宽到也匹配它,**不带夹具跑,check-guards 仍然全绿** ——
因为今天的网关里根本没有这个写法。我据此一度判定「这条对照证明不了任何事」。
把夹具摆上再跑,立刻红。

⇒ **判据**:验一条反向对照时,先确认那个合法形态**此刻存在于工作区**
(真实代码里有,或夹具摆上了),再去放宽规则。
否则你验的是「仓库里没有这种写法」,不是「这种写法不会被误报」。

⚠️ 顺带一条推论:**反向对照的价值与它守的形态在仓库里的稀有度成正比。**
真实代码里到处都是的形态,放宽规则立刻炸,不太需要对照钉着;
而「今天一个实例都没有、明天可能写出来」的形态,只有夹具能守。

### ★ 批量 agent 作业的返回计数不可信,必须独立核对产物

> **「死了」与「什么都没做」,在计数上是同一个数。**

**判据**:批量 agent 作业(workflow / 并行 subagent / 任何 fan-out)结束后,
**不采信它自报的完成计数**。必须用一条与 agent 无关的通路独立核对实际产物 ——
`pnpm typecheck`、`git status`、文件存在性,任一即可。

**⚠️ 计数为 0 尤其要查。** 它同时覆盖三种互不相同的情况:

| 真实情况                             | 计数  | 产物                    |
| ------------------------------------ | ----- | ----------------------- |
| 全部没跑(被跳过 / 被拒)              | 0     | 无                      |
| 全部跑了但都失败                     | 0     | 无                      |
| **跑了、改了文件、死在返回结果之前** | **0** | **有,而且可能是半成品** |

第三行是真正危险的那个:**工作区已经变了,而报告说什么都没发生。**

#### 实例(V0.9.0 Session 2,同一会话内两次)

| #   | 报告                                   | 实际                                                                         |
| --- | -------------------------------------- | ---------------------------------------------------------------------------- |
| 1   | `confirmed: 0` —— 「没有守卫惩罚记录」 | 27/44 个 agent 撞会话额度身亡;唯一跑完的那个**已实证**深链守卫会拦讲解性注释 |
| 2   | `converted: 0` —— 「一屏都没改」       | **5 个屏幕已改完并编译通过**,7 个 agent 全部死在返回之前                     |

第 2 次尤其能骗到人:若采信 `converted: 0` 而重跑一遍,
七个 agent 会在**已经改过的文件**上再改一遍 —— 而它们看到的是别人的产出,
不是原始的 kit。

⚠️ 第 1 次还带出一个更贵的形态:一个 agent 被杀在
`renameSync(dir, stash)` 与 `renameSync(stash, dir)` **之间**,
`console-web/` 整个从工作区消失。而后续 `check-guards` 报的是
「`console-web/src` 不存在」「登记了不存在的项目」——
**两条都指向症状,一条都没指向原因**;顺着它们改会把两处本来正确的东西改坏。

⇒ 所以核对的不只是「做了多少」,还有「**有没有留下不该留的东西**」:
`git status` 是这一条里最便宜也最有效的一步。

**它与「一个报错的工具与一个不报错的工具同样需要被核对」是同族** ——
那条说的是工具的**结论**会骗人,这条说的是工具的**计数**会骗人;
而计数更容易被当成事实,因为它看起来是个客观的数。

### ★ 守卫不能惩罚记录

> **一条会拦住「解释这个 bug」的守卫,会让人把解释删掉 —— 那比 bug 本身贵。**

**判据**:**任何守卫** —— 无论按文本判定还是按位置判定 ——
落地前必须先确认一件事 ——

> **它会不会把「对这条守卫的说明」当成违规对象?**

因为一条守卫要拦的东西,恰恰是最值得在注释、决策文档、任务书里**写下来**的那个。
两者在文本上一模一样,而语义相反:一处是**在犯**,一处是**在讲这是错的**。

⚠️ **判据不能只写「文本形状」。** 本条最初就是那么写的,而第三个实例
(见下)当场超出了那个范围:那条守卫**不看文本,只看位置** ——
说明书被判成违规,是因为它**待的地方**像违规对象,不是因为它**写的字**像。

| 判定方式   | 说明书为什么会被点名            | 实例                                                   |
| ---------- | ------------------------------- | ------------------------------------------------------ |
| **按文本** | 它复述了被禁的写法              | 回执守卫 · 约束 1 的路由守卫                           |
| **按位置** | 它落在被扫描的目录 / 命名形状里 | `checkSdkHasSyncAssertion` 把 `sdk/docs/` 当成一个 SDK |

⇒ 落地前问的那句话里,**不要出现「文本」二字**:
问的是「说明会不会被当成违规对象」,不是「说明的字面会不会匹配」。

#### 实例(V0.9.0,回执守卫第一次跑)

新加的「成功回执不得在 catch 之外」守卫,第一次运行就报出一处违规 ——
`CodeRef.tsx:18`,而那一行是 JSDoc:

```
 * 1. `setDone(true)` 在 try **之外**,失败时照样翻成 check、……
```

那段注释是**修复的一部分**:它记着原版为什么错、错在哪两层。
守卫却把它判成了错误本身。

⇒ 修法是在扫描时跳过注释行(`*` / `//` / `/*` 开头),
并配一条反向对照(`verify-guards.mjs` 的 33i):
夹具里放一段描述该反面写法的注释,**确认放行**。

#### 为什么它值一条独立规则,而不是「误报的一种」

上一节说过误报的代价是「人学会绕过这条检查」。**这一族更糟**:
人不会去绕过它,人会**照它说的改** —— 而唯一能改的就是把那段解释删掉。
于是守卫成功地让仓库变得更难懂,并且**全程显示为绿**。

| 普通误报           | 惩罚记录的误报            |
| ------------------ | ------------------------- |
| 人学会忽略这条规则 | **人服从这条规则**        |
| 规则失效,代码没坏  | 规则生效,**文档被删干净** |
| 事后能看出被绕过   | 事后什么痕迹都没有        |

⚠️ 这一族最容易长在**本仓最看重的那类文件**上 ——
`docs/DECISIONS/*`、CLAUDE.md 自己、以及每个「⚠️ 这里为什么不能那样写」的注释。
它们的存在理由就是复述反面形状。

#### 第三个实例(V0.9.0 Session 2):按**位置**判定的那一种

`checkSdkHasSyncAssertion` 认「一个 SDK」的判据是 **`sdk/` 下的任何目录**。
而 `sdk/docs/` 里只有一个文件 —— `why-sync-assertion.md`,
一份 104 行、讲**这条守卫为什么存在**的裁决文档。

于是它被判成「缺 `render.ts` 的 SDK」,PR 阻塞。

**这一处的文本里没有任何违规形状** —— 它甚至根本不是代码。
它被点名,纯粹因为它待在 `sdk/` 下面。

⇒ 判据改成「**有 `package.json` 的目录才是一个 SDK**」,
与 `pnpm-workspace.yaml` 认包的方式一致。

#### 第四个实例:守卫把**别的东西**当成了自己的对象

同一轮里,任务书诚实性守卫把交付行中的 `` `/v1/workspaces*` ``
当成了仓库路径,报「这个路径不存在」。

它不是「惩罚记录」的严格形态(那一行不是在讲这条守卫),
但根子一样:**守卫对「什么算我的对象」判得太宽**。
误报的代价也一样 —— 按它去改,会把一行准确的描述改坏。

⇒ 收窄:仓库相对路径不以 `/` 开头。API 端点、包名、URL 一律不算。

---

**落到操作上**,两步:

1. **文本维度**:先 grep 一遍仓库里**讲这个形状**的地方
   (注释、`docs/DECISIONS/*`、任务书),确认一处都不会被点名。
2. **位置维度**:列出这条守卫的**扫描范围**,然后问 ——
   「这个范围里,有没有**不是被测对象**的东西?
   尤其是**讲这条守卫的文档**?」

点到了就说明判据缺一个维度:文本上缺「这是说明,不是代码」,
位置上缺「这是文档目录,不是被测目录」。

### ★ 凡「改一处再看结果」的验证,先断言那一处真的被改到了

> **锚点失配是静默的** —— 没改到的那次,结果与「改了但没影响」长得一模一样。

同一族已经出现四次,伪装各不相同:

| #   | 形态                                           | 没改到时的表现                    |
| --- | ---------------------------------------------- | --------------------------------- |
| 1   | 压缩脚本 `s.replace()` 未命中却打印 ✅         | 状态**静默退回**,文档与实际不符   |
| 2   | 探针 10 第一版判据太弱                         | 变异生效了,断言却抓不到           |
| 3   | 判据写成 `includes(夹具路径)`,而**输出被截断** | 守卫其实报了 14 处违规,判据看不到 |
| 4   | `s.split(X).join(Y)` 未命中,却当成「已放宽」   | 「放宽后仍绿」——**结论完全反了**  |

第 3、4 都发生在写这一节的同一轮里,而且都差点导致**按误判去改一个本来正确的东西**。

**具体要求**,两条都要:

1. **断言变异生效**:`if (mutated === pristine) 报错`。报的是「锚点失配,本条结论作废」,
   **不是**「被测工具漏报」—— 两者的修法完全相反。
2. **判据要打在真正的结论上**,不是打在输出的某个片段上。
   例:要判「守卫会不会红」,就看**退出码**;`includes(某段文字)` 会被
   输出截断、措辞调整、多语言化各打败一次。

⚠️ 这条与「一个报错的工具与一个不报错的工具同样需要被核对」是一对:
那条管**结论对不对**,这条管**实验做没做成**。
实验没做成而当成做成了,是最省事的一种自欺 —— 它连一条错误信息都不会留下。

#### 四种具体形状(V0.9.0 实测,前三种发生在**同一条验证**上)

「先核对红对不对」这句话下一个人不会重新发现,但**这四种具体形状他会重新踩**:

| #   | 判据怎么写的            | 为什么错                                                    | 表现                           |
| --- | ----------------------- | ----------------------------------------------------------- | ------------------------------ |
| 1   | `marker.test(整份输出)` | 匹配到守卫**自己的通过行**(`通过  密码体系` 里就有它的名字) | 7 条守卫被误判成「惩罚了记录」 |
| 2   | 只看**含夹具名**的行    | 违规的**标题行不含文件名** —— 文件名在下一行的明细里        | 6 条真违规被误判成漏报         |
| 3   | 按**行**匹配            | 多条约束**共用一个标题**,区分它们的标记只在明细行           | 3 条前端约束被误判成漏报       |
| 4   | 夹具摆在一个位置        | **守卫的扫描范围各不相同**                                  | 范围之外的守卫显示为漏报       |

第 1、2、3 条的共同点:**输出是分层的**(标题行 / 明细行),
而结论落在哪一层随守卫而异。⇒ 稳妥的判据是按**块**匹配 ——
一个违规块 = 标题行 + 其后的缩进明细,两层一起看。

⚠️ **第 4 条要单独记,因为它长得完全不像判据问题。**
「凭据取值泄漏」只扫 `gateway/`,夹具放在 `packages/` 下,输出里干干净净 ——
读起来就是「这条守卫放过了真违规」。

> **那不是漏报,是夹具没摆到守卫看得见的地方。**

两者在输出上无法区分,而修法完全相反:前者要去改守卫,
后者要去挪夹具 —— 顺着第一种去改,会把一条本来正确的守卫改松。

⇒ 落到操作上:**一条负向验证报「没红」时,先确认被测守卫的扫描范围
覆盖了夹具的位置。** 范围写在守卫自己的 `collectFiles(p('...'))` 那一行,
读一眼比猜一轮便宜得多。

### ★ 推论:凡遍历集合做断言处,必须断言「**真的断言过至少一次**」

> **一个遍历零个元素的循环,与一个没有断言的测试等价** —— 而它在输出里
> 显示为「通过」。这是上面那条元规则最常见的一种具体形态。

**判据在出口,不在入口。**

⚠️ 这条最初写成「必须有**集合非空**的前置断言」。**那个判据不够** ——
它检查的是「入口有没有东西」,而真正要保证的是「**出口有没有发生**」。
嵌套 `continue` 过滤时两者会分家:外层集合非空,内层却可能一次都没执行到
`expect`,而前置断言照样通过。

**同一形状已经出现四次**,每次的伪装都不同:

| #   | 形态                                       | 空跑的表现                                     |
| --- | ------------------------------------------ | ---------------------------------------------- |
| 1   | `median([])` 返回 `NaN`                    | NaN 参与阈值比较恒为 false → **永远不超标**    |
| 2   | `unchanged` 只出现在一个分支上             | 别处读到 `undefined` → 诊断信息挑错的那条      |
| 3   | 进度标记脚本 `s.replace()` 未命中却打印 ✅ | 状态**静默退回**,文档与实际不符                |
| 4   | `planned` 恒为空,循环从 V0.4.0 起是死代码  | 「planned 端点返回 501」**两个版本没被验证过** |

四次的共同点:**「零次」与「全部通过」在输出上没有区别**。

**具体要求:数真正断言过几次**

```ts
// ❌ 循环零次而测试「通过」
for (const route of ROUTES.filter((r) => r.status === 'planned')) { … }

// ⚠️ 不够 —— 只保证入口有东西,不保证出口发生过
const planned = ROUTES.filter((r) => r.status === 'planned')
expect(planned.length).toBeGreaterThan(0)
for (const route of planned) {
  if (someCondition) continue   // ← 全部 continue 掉,断言仍是零次
  expect(…)
}

// ✅ 数出口
let asserted = 0
for (const route of ROUTES) {
  for (const [status, response] of Object.entries(route.responses)) {
    if (Number(status) >= 300) continue
    asserted += 1
    expect(…)
  }
}
expect(asserted, '一条都没断言到 —— 本条空跑了').toBeGreaterThan(0)
```

**什么时候可以只写前置断言**

循环体里**没有任何 `continue` / 提前 `return` / 条件分支**时,
「入口非空」等价于「出口发生」,写哪个都行 —— 这时前置断言更省事,也更早报错。

**一律不需要的**:遍历**字面量数组**(`for (const x of ['a','b'])`)——
它构造上非空且无过滤。

**一律需要的**:遍历**派生集合**(filter / 导入的常量 / 响应体 / 枚举 options),
或循环体内有任何过滤。

⚠️ **不要让链条无限延伸。** 「谁验证验证者」不必递归到底:
到**「这一层失效时会有人看见」**为止即可。判据是**可见性**,不是层数 ——
`verify-guards` 自己没有验证者,但它一失效,`check:all` 的输出里会少掉三十行,那是看得见的。

**提交**:Conventional Commits(`feat:` / `fix:` / `docs:` / `chore:`);PR 需含描述 / 影响范围 / 测试方式。

**分支**:`main` / `feature/v<版本号>`。**`main` 始终是主干,feature 分支合并后即删除。**

> ⚠️ 这条是 2026-08-16 补的,因为它被违反过一次而没人发现:开发从 V0.1.0 一路
> 做到 V0.4.6 都留在 `feature/v0.1.0` 上,`main` 停在 V0.2.0 中段落后 36 个提交,
> 而分支名说的是 v0.1.0、内容却是 v0.4.6。
>
> 后果不只是难看:`scripts/check-contract.mjs` 拿 **`main` 上的 openapi.json**
> 当契约冻结基线。`main` 停在半年前,基线就停在半年前 —— 冻结检查比对的是一个
> 早已不代表主干的快照,**它报的「破坏性 0 处」是对着错误的参照物说的**。
>
> 合并回 `main` 用 `--ff-only`,保持历史线性;快进不了说明分支有分叉,
> 停下查清楚,不要改用普通 merge 或 rebase 掩盖过去。

---

## 七、安全与隔离

**隔离级别不是配置偏好,是安全等级。** Harness agent 能执行 shell、读写文件系统。

| 级别                      | 适用                    | 状态          | 说明                                 |
| ------------------------- | ----------------------- | ------------- | ------------------------------------ |
| 逻辑(单进程)              | 🚨 **仅限单 principal** | ✅ 默认       | 多 principal 时**根本没有隔离**,见下 |
| 进程(一 principal 一进程) | **多用户的唯一可选项**  | ✅ **V0.4.5** | 不防内核提权、不限资源、不隔离网络   |
| 容器(进程 + OS 沙箱)      | 多租户 SaaS             | 📋 仅配置位   | 实现交给部署方的编排系统             |

> 🚨 **V0.4.7 起,逻辑档只支持单个 principal。** 本表前一版写的是
> 「逻辑 = 仅限互相信任的用户」——**那个判据是错的,而且错得不明显**。
>
> 「互相信任」说的是**恶意方能不能越界**;而逻辑档下多 principal 的问题是
> **根本没有分隔** —— principal 到不了 agent 执行层,所有人的文件都落进
> `anonymous/anonymous/`,互相静默覆盖。再互相信任的同事也不接受这个。
>
> 原因是架构限制而非待办(四条路全部走不通,见
> `docs/DECISIONS/principal-scope-binding.md`)。配置层已闸掉:
> 逻辑档 + 多用户身份**拒绝启动**。

> ⚠️ **进程隔离与 cancel 无关,方向甚至是反的。** 本表曾写着进程隔离「顺带解决
> 上游 stdio SDK 协议无 cancel 的问题」——**那句话是错的**。V0.2.0 Session 0 实测:
> **进程内**的 `Agent` 接口有 `cancel(cause)`,`AgentHandle.dispose()` 亦然,
> 两者都真的截断输出,网关的取消从 V0.2.0 起就能用
> (`docs/FEASIBILITY-REPORT-V2.md` §4.1)。「无 cancel」只适用于走 stdio SDK 协议的消费方。
>
> 反过来说,**进程隔离把一个已经好用的取消变成了需要重新解决的问题** ——
> 子进程里的 agent 不再有进程内句柄可调。V0.4.5 Session 0 验证了它可解
> (IPC 送取消 + 子进程内调进程内 cancel,真的截断),Session 2 实现了三级降级。
> 这是这一版的**代价**,不是收益。见 `docs/FEASIBILITY-REPORT-V45.md` §3。

> ⚠️ **进程隔离不是容器。** 它不防内核提权、不限制 CPU/内存、不隔离网络,
> 也不隔离同一 principal 的多个会话(它们共用一个进程)。代价是实测
> 冷启动 ~115 ms、常驻 ~58 MB/进程 —— `maxProcesses` 是必需配置而非调优项。
> **默认仍是逻辑隔离,进程隔离要显式开。**

- README 与文档必须**显著声明**逻辑隔离的适用边界。宁可劝退采用者,不要让他们从事故中学会。
- SCIM 令牌与 Admin 令牌**分离签发**:供给系统只能写身份镜像,不能读用量与凭据配置。
- Admin API Key **按租户签发**,一把钥匙不得横跨租户。
- 所有 Admin 与 SCIM 调用进入 `@dshwar/audit`,记录调用者 / 目标 / 变更前后。
- 沙箱策略喂给上游 `sandbox-policy` / `fs-sandbox`,**不另起炉灶**。
- **同一用户的不同工作区之间也是隔离的**,但隔离级别与租户间相同——工作区分区解决的是「按项目分开干活」这个组织问题,不是信任问题
- **不做运行时审批弹窗**。上游 SDK 协议的 server→client 请求是死能力;审批走**策略预授权 + 事后审计**,拒绝进 `@dshwar/audit`

---

## 八、开源与商业边界

**MIT 开源**:全部运行时插件、API 平面、控制平面核心、`billing-local`(只记账不收款)、`billing-stripe`(Stripe 适配器,**D4 改开源**,V0.6.0)。

**闭源**:仅托管服务本体 —— `billing-hosted`(托管收款,含微信 / 支付宝等国内通道)与 DSHWAR Cloud。

> D4(2026-08):支付适配器没有护城河价值,闭源它等于让自建者收不了钱,
> 直接违背「开源用户拿到可用的完整基座」。开源许可不可撤回 ——
> 回滚窗口只到 `@dshwar/billing-stripe` 首次发布前,见 AUTOPILOT-LOG 的 D4 专节。

开源用户拿到的是**可用的完整基座**;商业客户买的是省掉自建的时间。这条线公开写明,藏着会失去信任。

- **客户端分发**:开源版由项目签名(Windows 走 SignPath Foundation 免费通道,macOS 走 Apple Developer $99/年);**客户白牌版本由客户自己签名**——界面挂客户品牌,签名主体就该是客户。DSHWAR 只提供打包与签名的 CI 模板,不代签

---

## 九、商标与声明

- 项目名不含 "DeepSeek"。README 必须声明:非 DeepSeek 官方产品、无隶属关系。
- 对上游的引用限于**指名性使用**(识别所扩展的项目),不得用于品牌暗示。
- 发布前过一次法务。
